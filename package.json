{
  "name": "pocket-save",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "npm": "7.10.0",
    "node": "16.0.0"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && npm install && node ./index.js",
    "buid": "cd client && npm install && npm run build",
    "dev": "concurrently \"cd client && npm start\" \"cd server && nodemon ./index.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.1.0"
  }
}
